# Archive

Historical material kept for reference. Not part of the active build or test solution (`FastFsm.Net.slnx`).

- `oldtests/` — pre-recovery test suite and legacy machine definitions (superseded by `FastFsm.Tests`, `Machines.Tests`, and related projects on `main`).
