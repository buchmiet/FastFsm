# Benchmark snapshot: `win-arm64` (Windows 11 ARM64, Orange Pi 5 Max)

| Field | Value |
|-------|-------|
| Date | 2026-08-21 |
| Commit | `d961623` |
| Package | FastFsm.Sharp 0.9.2 (project references) |
| Job | InProcess, `IterationCount=15`, `WarmupCount=3`, BenchmarkDotNet 0.15.8 |
| .NET | SDK 11.0.100-preview.7 (rollForward from `global.json` 10.0.100) |

## Interpretation

These are framework microbenchmarks on small synthetic workloads. Per-operation values are amortized over batched invocations and are not application-level transition latency measurements.

## Observability flat (512 ops/invoke)

| Scenario | Mean/op | Alloc/op |
|----------|--------:|---------:|
| Baseline (no extension) | 31.3 ns | 0 |
| All disabled | ~31 ns | 0 |
| Metrics only | 221.4 ns | 0 |
| Tracing, no listener | 269.7 ns | 0 |
| Sampled tracing + listener | 2.24 µs | 1.26 KB |

The sampled-tracing case includes real `Activity` creation; the no-listener case does not.

## Notes

- Full `StateMachineBenchmarks` / `HsmBenchmarks` comparison tables were blocked on this host by `DisassemblyDiagnoser` failures under InProcess on WoA; disassembly is disabled in a follow-up commit for cross-platform runs.
- HSM observability baseline ~34 ns, metrics-only ~213 ns on the sibling-toggle workload.
